services:
  app:
    build: .
    container_name: ptit-conduct-app
    ports:
      - "3000:3000"
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./uploaded_files:/app/uploaded_files
